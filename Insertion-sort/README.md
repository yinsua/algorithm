#插入排序
##语句分析

1. 定义从0到列表长度减一的长度的循环
2. 定义一个变量代表当前数字的指向
3. 定义一个变量代表指向的前一个数字
4. 如果前一个指向大于0（没有超出范围）且前一个数字大于后一个数字，则
5. 将前一个的数字赋值给后一个数值（这里是相对的，不是绝对的，不能简单替换为after）
6. 前一个值自减一，并跳到步骤4继续循环
7. 循环结束，将后一个值（after）给前一个值（这里的这个值可能经过多次变换得到的，因为front可能一直在变化）
8. 跳转至步骤1，直至循环结束